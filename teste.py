numerateur = float(input('saisir ici : '))
deno =float(input('saisi ici : '))
if deno != 0 :
    quotient = numerateur / deno
    print('le quotient est egale a :',quotient)
else:
    print('le deno ne peut etre egale a 0')